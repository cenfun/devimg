import './globals.css';

export const metadata = {
    title: 'devimg',
    description: 'Generated by devimg'
};

const className = '';

export default function RootLayout({ children }) {
    return (
        <html lang="en">
            <body className={className}>{children}</body>
        </html>
    );
}
